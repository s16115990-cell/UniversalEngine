(function (window) {
    "use strict";

    const EngineConfig = {
        name: "UniversalEngine",
        version: "1.0.0",

        autoStart: false,

        targetFPS: 60,

        debug: true,

        maxEntities: 10000
    };

    window.EngineConfig = EngineConfig;

})(window);
